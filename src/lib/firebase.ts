
import { initializeApp, getApp, getApps, type FirebaseApp } from 'firebase/app';

// This file is now a simple pass-through for Firebase SDK functions.
// All initialization logic, including environment variable checks, has been moved
// to the FirebaseAnalytics component to ensure it only runs on the client-side
// after the environment is fully loaded.

export { initializeApp, getApp, getApps, type FirebaseApp };
